# App Factory Production Gitignore
# Conservative approach: better to ignore too much than leak sensitive data

# Updates folder - internal marketing/social content drafts
updates/

# Pipeline execution logs and build artifacts
**/outputs/
**/builds/
**/dapp-builds/
**/website-builds/
**/runs/
*.log
*.tmp

# Coverage reports
coverage/
.nyc_output/

# Runtime & local artifacts - never commit user run data
.appfactory/
.state/
.cache/
.tmp/
test_claude_logs/
**/*.exitcode
**/*.stderr.log
**/*.stdout.log

# Environment & secrets - never commit keys or tokens
.env
.env.*
!.env.example
!.env.example.template
*.key
*.pem
*.p12
*.pfx
*.crt
*.cert
*.secret
*.secrets
*token*
*credentials*
*apikey*
secrets.json
secrets.yaml
secrets.yml
.secrets/

# Private keys and certificates
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
*.ppk

# AWS credentials
.aws/credentials
aws-credentials*

# Google Cloud credentials
*-credentials.json
*-service-account.json
gcloud-*.json

# Terraform state (may contain secrets)
*.tfstate
*.tfstate.*
.terraform/

# Mobile app build artifacts
node_modules/
.expo/
.expo-shared/
ios/
android/
*.keystore
*.jks

# Claude / AI tooling - local AI artifacts
.claude/*
!.claude/commands/
!.claude/skills/
!.claude/hooks.toml
.claude-cache/
claude.log
*.prompt.log

# OS & editor noise - platform-specific files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Test & debug artifacts - development-only files  
test-output/
debug/
*.trace
*.log

# User-specific or machine-specific data
# Any file generated during local pipeline runs
# Any cached model outputs  
# Any active run tracking state

# XDG compliance - local config/cache directories
.config/appfactory/
.local/share/appfactory/
.cache/appfactory/

# Dashboard build artifacts (additive-only for isolation)
dashboard/node_modules/
dashboard/dist/
dashboard/.vite/
dashboard/public/leaderboard.json

# Preview service artifacts
preview/node_modules/
preview/.runtime/
preview/dist/
preview/*.log

# Rig framework reference (cloned, not tracked)
references/rig/

# Demo output artifacts
demo/out/

# Internal App Factory branding assets (not for public distribution)
demo-video/.internal/
demo-video/public/internal-audio/

# Playwright MCP artifacts (regenerated on each run)
.playwright-mcp/
