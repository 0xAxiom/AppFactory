# App Factory Production Gitignore
# Conservative approach: better to ignore too much than leak sensitive data

# Pipeline execution logs (but NOT runs/ or builds/ - we want those tracked)
**/outputs/
*.log
*.tmp

# Runtime & local artifacts - never commit user run data
.appfactory/
.state/
.cache/
.tmp/
test_claude_logs/
**/*.exitcode
**/*.stderr.log
**/*.stdout.log

# Environment & secrets - never commit keys or tokens
.env
.env.*
!.env.example
*.key
*.pem
*.secret
*token*
*credentials*

# Mobile app build artifacts
node_modules/
.expo/
.expo-shared/
ios/
android/
*.keystore
*.jks

# Claude / AI tooling - local AI artifacts
.claude/
.claude-cache/
claude.log
*.prompt.log

# OS & editor noise - platform-specific files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Test & debug artifacts - development-only files  
test-output/
debug/
*.trace
*.log

# User-specific or machine-specific data
# Any file generated during local pipeline runs
# Any cached model outputs  
# Any active run tracking state

# XDG compliance - local config/cache directories
.config/appfactory/
.local/share/appfactory/
.cache/appfactory/

# Dashboard build artifacts (additive-only for isolation)
dashboard/node_modules/
dashboard/dist/
dashboard/.vite/
dashboard/public/leaderboard.json

# Preview service artifacts
preview/node_modules/
preview/.runtime/
preview/dist/
preview/*.log
