# Required: OpenAI API key for GPT-4
OPENAI_API_KEY=sk-your-api-key-here

# Optional: HTTP server port (default: 8080)
PORT=8080

# Optional: Maximum tool iterations per request (default: 10)
MAX_TOOL_ITERATIONS=10

# Optional: Maximum file size to read in KB (default: 500)
MAX_FILE_SIZE_KB=500

# Optional: Logging level (default: info)
LOG_LEVEL=info

# Optional: Comma-separated list of allowed root directories
# If not set, any directory can be explored (use with caution)
# ALLOWED_ROOTS=/Users/dev/projects,/home/user/code
