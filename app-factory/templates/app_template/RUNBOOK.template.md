# {{APP_NAME}} - Runbook

**Exact steps to run this app locally.**

---

## Prerequisites

- **Node.js** 18+ installed
- **npm** or **yarn** installed
- **Expo Go** app on your phone (optional, for physical device testing)
- **Xcode** (for iOS Simulator on macOS)
- **Android Studio** (for Android Emulator)

Verify prerequisites:

```bash
node --version   # Should be v18+
npm --version    # Should be v8+
```

---

## Step 1: Install Dependencies

```bash
cd {{APP_SLUG}}
npm install
```

Expected output:

```
added XXX packages in XXs
```

If you see peer dependency warnings, they're usually safe to ignore. If install fails:

```bash
npm install --legacy-peer-deps
```

---

## Step 2: Start Development Server

```bash
npx expo start
```

Expected output:

```
Starting Metro Bundler
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above to open the project in Expo Go
› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web
› Press j │ open debugger
› Press r │ reload app
› Press m │ toggle menu
```

---

## Step 3: Open on Device/Simulator

### iOS Simulator (macOS only)

Press `i` in the terminal, or:

```bash
npx expo start --ios
```

Expected: Simulator launches, app loads.

### Android Emulator

Press `a` in the terminal, or:

```bash
npx expo start --android
```

Expected: Emulator launches, app loads.

### Physical Device

1. Install Expo Go from App Store or Play Store
2. Scan the QR code shown in terminal
3. App loads on your device

---

## Step 4: Verify App Loads

After launching, you should see:

1. **Splash screen** appears briefly
2. **Onboarding screens** (first launch only)
3. **Home screen** with main functionality

If you see a red error screen, check the Metro bundler terminal for error details.

---

## Common Issues

### "Metro bundler connection failed"

```bash
npx expo start --clear
```

### "Unable to find iOS simulator"

Open Xcode > Preferences > Locations > Command Line Tools (select a version)

### "ADB not found" (Android)

Ensure ANDROID_HOME is set:

```bash
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

### "Module not found" error

```bash
rm -rf node_modules
npm install
npx expo start --clear
```

---

## Development Commands

| Command                      | Purpose                         |
| ---------------------------- | ------------------------------- |
| `npx expo start`             | Start dev server                |
| `npx expo start --clear`     | Start with cache cleared        |
| `npx expo start --ios`       | Start and open iOS simulator    |
| `npx expo start --android`   | Start and open Android emulator |
| `npx expo install <package>` | Install Expo-compatible package |

---

## Next Steps

1. Run through the app to verify functionality (see TESTING.md)
2. Check LAUNCH_CHECKLIST.md for deployment preparation
3. Review ASO materials in `aso/` before store submission

---

**Generated by App Factory** | [Factory Ready Standard](../../docs/FACTORY_READY_STANDARD.md)
