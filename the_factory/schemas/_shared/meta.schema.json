{
  "type": "object",
  "properties": {
    "meta": {
      "type": "object",
      "properties": {
        "run_id": {
          "type": "string",
          "description": "Unique identifier for the run that generated this stage"
        },
        "idea_id": {
          "type": "string", 
          "description": "Unique identifier for the specific idea this stage belongs to"
        },
        "idea_name": {
          "type": "string",
          "description": "Human readable name of the idea"
        },
        "idea_dir": {
          "type": "string",
          "description": "Directory name for this idea pack"
        },
        "source_root": {
          "type": "string",
          "description": "Root path for this idea pack"
        },
        "input_stage_paths": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Explicit list of files read as input to this stage"
        },
        "boundary_path": {
          "type": "string",
          "description": "Path to boundary.json file for isolation enforcement"
        }
      },
      "required": ["run_id", "idea_id", "idea_name", "idea_dir", "source_root", "input_stage_paths", "boundary_path"]
    }
  },
  "required": ["meta"]
}